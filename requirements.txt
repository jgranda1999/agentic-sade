# OpenAI Agents SDK (provides the 'agents' module: Agent, Runner, trace)
openai-agents>=0.9.0
# OpenAI API client (dependency of openai-agents; explicit for API calls)
openai>=2.9.0,<3
# Pydantic (used by models.py and the SDK)
pydantic>=2.12.0,<3
